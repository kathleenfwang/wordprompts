<%= react_component 'ViewPost', { post: @post, user: current_user } %>
 
<% if current_user.username == @post.user.username %>
 
<button id ="delete"> <%= link_to("Destroy", "/posts/#{@post.id}", method: :delete) %> </button>
 

<% end %> 